// Placeholder main entry — task 01 will flesh this out. Kept here only so
// the worktree can typecheck as a complete project.
import { app } from 'electron';

app.whenReady().then(() => {
  console.log('[main] ready');
});
